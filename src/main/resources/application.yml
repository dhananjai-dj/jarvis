spring:
  ai:
    openai:
      base-url: https://api.groq.com/openai/v1
      api-key: ${API_KEY}
      chat:
        model: qwen/qwen3.8-27b
        max-retries: 1
    retry:
      max-attempts: 1
  datasource:
    url: jdbc:postgresql://localhost:5432/jarvis
    username: dhananjai_dj
    password: Ammu123*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379